# 每 5 分钟同步一次（粘贴到 crontab -e）
# 需先创建 venv: cd /opt/stacks/bookmark-sync && python3 -m venv .venv
*/5 * * * * /opt/stacks/bookmark-sync/.venv/bin/python /opt/stacks/bookmark-sync/sync.py >> /opt/stacks/bookmark-sync/logs/sync.log 2>&1
